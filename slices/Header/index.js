import React from 'react'
import { PrismicRichText } from '@prismicio/react'

const Header = ({ slice, context }) => {
  // const logo = slice.primary.logo
  // const buttonOne = slice.primary.button1.data
  // const buttonTwo = slice.primary.button2.data

  // const buttonArray = [buttonOne, buttonTwo].map((btn) => {
  //   const { buttonType, color, label, href: url, type } = btn
  //   return { url, buttonType, label, color, type }
  // })

  return (
    <>
      <h1>This is header</h1>
    </>
  )
}

export default Header
